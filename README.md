# myhzxsim

## *Cautions: Descriptions below are mostly under construction*
<br>

myhzxsim is a simulator of the HiZ-GUNDAM EAGLE (wide-field X-ray monitor) to demonstrate observations of various X-ray objects, taking into account the complex response functions of the Lobster-eye micropore optics (MPO) and CCD imager. <br>

The software code is written entirely in Python and has been tested in the Anaconda Python environment on Mac and Linux (Ubuntu).


<!-- Anybody can test it without any install on the Alibaba cloud server.
See the [step by step instruction](#step-by-step-instruction).
<br> -->

---

## Requirements

- Python including numpy, scipy, astropy (tested in python version 3.12)
- healpy (https://healpy.readthedocs.io/en/latest/index.html)
- HEASOFT including PyXspec (tested in heasoft version 6.35, 6.36)
- CALDB

<br>

---

## Major points
<!-- , difference from Haiwu's simulator -->

### Calibration datase (CALDB) for X-ray telesocpe and detectors

  In default, myhzxsim utilizes CALDB files of telescope definition (TELDEF), response matrix (RMF), and ancillary response (ARF) located in the directory, `refdata`.
  In the future, these files will be moved to the directory given by the  CALDB environment variable. <br>
  The tentative RMF CALDB is based on those for the eROSITRA pnCCD (ref.). 

### Point response functions of Lobster-eye MPO
  myhzxsim employs point response data of Lobster-eye MPO generated by Geant4 X-ray tracing simulations.
  To take account of stray lights coming from X-ray sources located outside of the FOV, the response data is extended to the image region twice larger than the nominal FOV.
  The data is stored in a directory, `simdb/g4evtpsff/`.<br>
  The effects of the support structures, the shadows of the frames and baffles, are calculated for each source position at the later observation simulation phase.<br>
  ***Somoothing***

### Soft X-ray sky background (emission)
  <!-- One default option.  Select the default or none.  -->
  
  Soft X-ray backgrounds consist of unresolved extragalactic sources and Galactic diffuse emissions that are thought to originate from local hot bubbles and Galactic halos. <!--These surface brightness and spectra have been measured in past observations, such as ROSAT all-sky survey.-->
  Using the [ROSAT all-sky survey data and its background tool (sxrbg) released via HEASARC](https://heasarc.gsfc.nasa.gov/Tools/xraybg_help.html), these X-ray spectra are modeled for individual 1x1 deg<sup>2</sup> sky regions pixelized by healpix of Nside = 64 (total 49152 pixels). The extragalactic component is known to be uniform and approximated by a power-law function with a photon index ~ 1.5. 
  The residual Galactic component was fitted with a thin-thermal plasma emission model.
  The obtained model data is stored in simdb/xrbkg_rayspec.fits. It is used in default.

### Non X-ray backgound (NXB)
 
  Non X-ray background (NXB) represents residual cosmic-ray events after discriminating these events with event grades (pixel hit patters), pulse-height upper/lower limits.

  ***The default file is refdata/fake_bkg_exp1e6s.pha***

### X-ray catalog sources

Two options. Select both, either, or none.

1. MAXI monitoring source<br>
  The MAXI team releases daily light curves of ~400 bright variable / flaring sources via the [MAXI home page](http://maxi.riken.jp/). <!--These targets are selected based on daily image. -->
  Among them, ~100 objects are constantly detected in a daily time scale.
  They are mostly Galactic X-ray binaries embedding black-hole or neutron stars. A half of them are located on the Galactic plane.
  These soft X-ray spectra suffer heavy interstellar absorption. <br>
  As for these 100 objects, MAXI light curves in 2-4 keV band and spectral models obtained from past observation results are utilized. <br>
  The catalog data is stored in a file, simdb/catmxmon.fits.


2. ROSAT all-sky source catalog (2RXS)<br>
  2RXS catalog is the latest (last?) release of the ROSAT all-sky source catalogs, including 135,000 sources with ~30% possible spurious detections (Boller+2016). Too bright objects (Sco X-1, …) and regions (Cygnus Vela, ..) are excluded.
  The data contains results of spectral fits with power-law, blackbody, and thin-thermal models to bright objects with enough photon statistics (roughly > 100 photons).<br>
  In the myhzxsim, power-law spectral models are utilized if these model parameters are available.
  These source data are collected in a file, simdb/cat2rxs_plparcut.fits.<br>
  Some of 2RXS objects are included in the MAXI catalog. In these objects, data in the MAXI catalog is used.

### Auxillary data

  To run simulation, attitude quaternion parameters are required.
  <!-- The definition is same as the official level-2 attitude data. -->

### Output file

myhzxsim generates a processed event FITS file. <br>
In addition to TIME, PI, X, Y, DETX, DETY, RAWX, RAWY columns, the output file includes the following optional columns.
| | |
|---|---|
|RA_OBJ  | Source sky position RA in degre (-999. for NXB)|
|DEC_OBJ | Source sky position Dec in degree (-999. for NXB)|
|PHOTON_E | Photon energy in keV   (-1. for NXB)|
| | |

<br>

---

## How to run myhzxsim  

### Step by step instruction

1. Setup environment variable<br>
  Set environment variables for anaconda python (version 3.11) and Heasoft (version 6.36). <br>
  Also, set HZXSIMDIR to point to the software directory.<br>
  <!-- A setup script (/NFS/mutsumi/ep/myhzxsim/setup_myhzxsim.sh) is like below. <br> -->

```
# This is a sample of myhzxsim setup file.
# Modify file paths according to your system. 

### Anaconda
. ~/anaconda3/etc/profile.d/conda.sh
conda activate

### Heasoft
export HEADAS=${HOME}/headas/heasoft-6.28/x86_64-pc-linux-gnu-libc2.17
. $HEADAS/headas-init.sh

## CALDB
export CALDB=${HOME}/caldb
export CALDBCONFIG=$CALDB/software/tools/caldb.config
export CALDBALIAS=$CALDB/software/tools/alias_config.fits

### HZXSIM
export HZXSIMDIR=${HOME}/myhzxsim

```

2. Copy a sample script and parameter files to your work direct <br>

```
cd test_work
cp $HZXSIMDIR/test_sample/*.* ./
```

3. Run test scripts<br>
Several examples to run simulations including Crab observations are shown in the below.<br>
Each run creates processed event files.<br>

```
$ ./myhzxsim.py
(Usage) myhzxsim.py (Obs parameter filename) [-s (X-ray source filename)] [-c (config filename)]
$
$ ./myhzxsim.py obsparam_crab.yml  
...

$ ./myhzxsim.py obsparam_crab.yml -s mytarget_grbs.yml 
...

$ ./myhzxsim.py obsparam_crab.yml -s mytarget_grbs.yml -c myhzxsim_myconf.yml
...
`
```

<br>
<br>
<br>


### Description of parameter files

#### Observation (simulation) parameter file

obsparam.yml

```
### Obseravation including Crab
- obsid: 100000            # used for output filename
  objectName: XRAYSKY      # used in output fits header
  tstart: 0.0              # start time in a mission time
  tstop:  1000.0           # stop time in a mission time
  qparam: -0.58883817, 0.00332964, -0.00195868, 0.80824173   # attitude quaternion
  detlist: range(1,17).    # list of detector id number to run simulation 1,2,...,16
  ##
  mxmoncat: True   # include MAXI catalog sources (True) or not (False)
  rxscat:   True   # include ROSAT catalog sources (True) or not (False)
  xrb: DEF         # diffuse X-ray background model (Default) or None
  nxb: DEF         # non X-ray background model (Default), user-specified filename or None


### Obseravation including Galactic center
- obsid: 100001
  objectName: XRAYSKY
  tstart: 0.0
  tstop:  1000.0
  qparam: -0.82470631,  0.00189276,  0.00301748, -0.56555001
  detlist: range(1,17)
  ##
  mxmoncat: True   
  rxscat:   True   
  xrb: DEF        
  nxb: DEF        

```

#### X-ray source file (optional)

mytarget_grbs.yml

```
- objectName: GRB1
  ra:  83.
  dec: 13.
  xsmodel: phabs*powerlaw
  xsparam: (0.3, 1.0, 1.0)
  lcfile: grb1_lc.fits
...
```

#### Coniguration parameter file (optional)

The default configuration file is $hzxSIMDIR/refdata/myhzxsim_default.yml

myhzxsim_myconf.yml 
```
###. HZXSIMDIR is replaced with the value of the environment variable $HZXSIMDIR. 

#############################
##### CALDB information #####
#############################

## TELDEF
teldefdir: HZXSIMDIR/refdata/teldef
teldefver: 20251216v002

### Ancilary response (effective area) 
#arffname: HZXSIMDIR/refdata/hzxarf_withframe_rn0.0nm_imgall.arf  ### surface roughness 0 nm
arffname: HZXSIMDIR/refdata/hzxarf_withframe_rn2.0nm_imgall.arf  ### surface roughness 2 nm

### Response matrix
rmffname: HZXSIMDIR/refdata/hzx_erosita.rmf

### PSF sigma in mm
#psf_sigma: 0.2  ### = 2.29 arcmin (sigma) = 5.38 arcmin (FWHM)
psf_sigma: 0.4  ### = 4.58 arcmin (sigma) = 10.8 arcmin (FWHM) 

### Non X-ray background spectrum
nxbspecfname: HZXSIMDIR/refdata/fake_bkg_exp1e6s.pha 

### X-ray sky background spectral data in healpix image
xrbdatfname: HZXSIMDIR/simdb/xrbkg_rayspec_erobin.fits

################################
##### X-ray source catalog #####
################################
### MAXI catalog file
catmxmonfname: HZXSIMDIR/simdb/catmxmon.fits
catmxmonlcdir: HZXSIMDIR/simdb/mxgsclc

### ROSAT catalog file
catrxs2fname: HZXSIMDIR/simdb/cat2rxs_plparcut.fits
```

<br>

---

## How to install myhzxsim in your machine

1. Install required software
    - Anaconda python (version 3.12)
    - Heasoft (version 6.36)
    - healpy
2. Copy all of myhzxsim directory trees.

<br>

---

## How to custermize simulations for your own studies

  1. Create / modify observation parameter file
  2. Create / modify X-ray source data
  3. How to simulatie transient/variable sources
